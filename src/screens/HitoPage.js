import { StyleSheet, Text, View } from "react-native";

export const HitoPage = () => {};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
